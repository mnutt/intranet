<h1>Editing comment</h1>

<%= error_messages_for :comment %>

<% form_for(hiring_comment_path(@comment)) do |f| %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', hiring_comment_path(@comment) %> |
<%= link_to 'Back', hiring_comments_path %>
