<h1>Listing comments</h1>

<table>
  <tr>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= link_to 'Show', hiring_comment_url(comment) %></td>
    <td><%= link_to 'Edit', edit_hiring_comment_path(comment) %></td>
    <td><%= link_to 'Destroy', hiring_comment_url(comment), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end  %>
</table>

<br />

<%= link_to 'New comment', new_hiring_comment_path %>
